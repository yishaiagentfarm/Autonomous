/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Autonomous",
  assetPrefix: "/Autonomous/",
  images: { unoptimized: true },
};
export default nextConfig;
